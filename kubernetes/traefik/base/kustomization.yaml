apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: traefik
    releaseName: traefik
    namespace: traefik
    version: 38.0.2
    repo: https://traefik.github.io/charts
    includeCRDs: true
    valuesFile: values.yaml

resources:
  - namespace.yaml
